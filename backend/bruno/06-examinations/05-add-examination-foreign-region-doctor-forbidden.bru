meta {
  name: 05 add examination foreign region doctor forbidden
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/patients/{{seedForeignPatientId}}/examinations
  body: json
}

headers {
  Authorization: Bearer {{doctorToken}}
  Content-Type: application/json
}

body:json {
  {
    "title": "Should fail",
    "examDate": "2026-04-13",
    "comment": "Forbidden scenario",
    "measurements": [
      {
        "characteristicCode": "weight_kg",
        "value": 70
      }
    ]
  }
}

tests {
  test("regular doctor cannot write to foreign region patient", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
