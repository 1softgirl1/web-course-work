meta {
  name: 13 patch examination foreign region forbidden
  type: http
  seq: 13
}

patch {
  url: {{baseUrl}}/api/patients/{{seedForeignPatientId}}/examinations/{{seedForeignExaminationId}}
  body: json
}

headers {
  Authorization: Bearer {{doctorToken}}
  Content-Type: application/json
}

body:json {
  {
    "title": "Should be forbidden"
  }
}

tests {
  test("regular doctor cannot patch foreign-region examination", function () {
    expect(res.getStatus()).to.equal(403);
  });
}
