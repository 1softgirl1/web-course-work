meta {
  name: 16 current doctor profile no token
  type: http
  seq: 19
}

get {
  url: {{baseUrl}}/api/doctors/me
}

tests {
  test("missing bearer cannot get doctor profile", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
