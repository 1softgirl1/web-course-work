meta {
  name: 09 login patient new password
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/auth/login
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "{{passwordChangePatientCode}}",
    "password": "password-change-patient-new"
  }
}

tests {
  test("new patient password works", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
