meta {
  name: 13 change password short new
  type: http
  seq: 13
}

post {
  url: {{baseUrl}}/auth/password/change
  body: json
}

headers {
  Authorization: Bearer {{changeDoctorToken}}
  Content-Type: application/json
}

body:json {
  {
    "currentPassword": "doctor-password-changed",
    "newPassword": "123",
    "refreshToken": "{{changeDoctorRefreshToken}}"
  }
}

tests {
  test("short new password returns 400", function () {
    expect(res.getStatus()).to.equal(400);
  });
}
