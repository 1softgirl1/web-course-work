meta {
  name: 05 login doctor new password
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "{{passwordChangeDoctorUsername}}",
    "password": "password-change-doctor-new"
  }
}

tests {
  test("new doctor password works", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
