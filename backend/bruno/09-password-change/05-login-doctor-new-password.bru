meta {
  name: 05 login doctor new password
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/auth/login
  body: json
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "{{doctorEmail}}",
    "password": "doctor-password-changed"
  }
}

tests {
  test("new doctor password works", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
