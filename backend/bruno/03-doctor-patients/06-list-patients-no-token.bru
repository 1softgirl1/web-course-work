meta {
  name: 06 list patients no token
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/doctor/patients?scope=own&page=0&limit=20
}

tests {
  test("missing token returns 401", function () {
    expect(res.getStatus()).to.equal(401);
  });
}
