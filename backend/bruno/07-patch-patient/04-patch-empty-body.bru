meta {
  name: 04 patch empty body
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/patients/{{createdPatientId}}
  body: json
}

headers {
  Authorization: Bearer {{doctorToken}}
  Content-Type: application/json
}

body:json {
  {}
}

tests {
  test("empty patch returns 400", function () {
    expect(res.getStatus()).to.equal(400);
  });
}
